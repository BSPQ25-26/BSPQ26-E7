package com.lablend.backend.entity;

/**
 * All possible roles a {@link User} can have in the system.
 * @version 1.0
 */

public enum UserRole {
    /** Administrator role */
    ADMIN,
    /** Regular user role */
    USER
}